import styled from 'styled-components/native';

export const PICTURE_SIZE = 38;
export const Picture = styled.Image`
  width: ${PICTURE_SIZE}px;
  height: ${PICTURE_SIZE}px;
  border-radius: 50px;
`;
